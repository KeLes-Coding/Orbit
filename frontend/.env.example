# Comma-separated hostnames allowed to access the Vite dev server.
# Keep real private domains in .env.local or your shell environment.
ORBIT_FRONTEND_ALLOWED_HOSTS=

# Optional browser-facing API base URL. Defaults to /api/v1.
VITE_API_BASE_URL=
